import styled from "styled-components";

export const Container = styled.div`

display: flex;
align-items: center;
flex-direction: row;
gap: 1.6rem;

  >div{
    display: flex;
    align-items: center;
    flex-direction: column;
    gap: 8px;
  }
`